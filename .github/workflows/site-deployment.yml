name: site-deployment

on:
  push:
    branches: 
      - dev
jobs:
  deploy:
    runs-on:
      - self-hosted
      - offtheclock
    
    steps:
      - name: Checkout
        uses: actions/checkout@v2
